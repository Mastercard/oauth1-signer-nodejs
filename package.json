{
  "name": "mastercard-oauth1-signer",
  "version": "1.2.0",
  "description": "Zero dependency library for generating a Mastercard API compliant OAuth signature.",
  "main": "src/OAuth.js",
  "types": "types/index.d.ts",
  "files": [
    "src",
    "types",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "test": "mocha && mocha --reporter mocha-sonarqube-reporter --reporter-options output=test-results.xml",
    "coverage": "nyc mocha && nyc report --reporter=text-lcov > .nyc_output/coverage.lcov"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Mastercard/oauth1-signer-nodejs.git"
  },
  "author": "Mastercard",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^10.6.0",
    "mocha": "^11.7.6",
    "mocha-sonarqube-reporter": "^1.0.2",
    "nyc": "^18.0.0",
    "sinon": "^22.0.0"
  },
  "overrides": {
    "serialize-javascript": "^7.0.7"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
